# Secrets
.env

# Dependencies
node_modules/

# Alfred's runtime state: memories, transcripts, session state, logs.
# All personal, all machine-local, none of it ever belongs in git.
# One rule guards the whole tree — resist adding exceptions to it.
agent/var/

# Editor scratch
*.swp
